# AGENTS.md

## プロジェクト概要

本リポジトリは、n8nを中心にGmail / Outlook / OpenAI / LINE / Google Calendar / Supabaseを連携するAIメールアシスタントです。詳細な運用方法は `docs/17_Codex運用計画.md` を参照してください。

## 作業方針

- GitHub Issueを作業キューとし、原則として1タスク・1ブランチ・1PRで完結させる。
- Issueの受け入れ条件を満たす最小限の変更に限定し、無関係なリファクタリングを行わない。
- 着手前に関連ドキュメントと既存実装を確認し、完了前に差分と確認結果を見直す。

## Git・PR

- ブランチは最新の基準ブランチから作成し、`codex/<issue番号>-<短い説明>` を基本とする。
- コミットメッセージは `<type>: <日本語の要約>` とし、`feat`、`fix`、`docs`、`test`、`chore` など適切なtypeを使う。
- PRは原則1つのIssueだけを扱い、タイトルはコミット規約に合わせる。
- PR本文に概要、変更内容、確認結果、関連Issue（`Closes #<番号>`）を記載する。自己レビュー後にReviewへ移す。

## 秘密情報と環境変数

- APIキー、トークン、パスワード、秘密鍵、個人情報をコード、ドキュメント、ログ、Issue、コミットへ含めない。
- `.env` は作成・編集・コミットしない。環境変数を追加する場合は、必要に応じて `.env.example` に変数名と安全なダミー値のみを記載する。
- n8nのcredential、OAuth情報、Supabaseのservice role keyなどの実値は各サービスの安全な保管先で管理する。

## ファイル別ルール

- 仕様・構成・運用が変わる場合は、同じPRで関連する `README.md` または `docs/` を更新する。
- n8n workflow JSONは、Issueで明示された場合のみ変更する。export前後の差分を確認し、credential IDや接続情報、実行データを含めない。
- Supabase migrationは、Issueで明示された場合のみ新しいmigrationとして追加する。適用済みmigrationの書き換えや履歴破壊を行わず、ロールバック方法と影響範囲を確認する。

## 確認

変更内容に応じて利用可能なコマンドを実行し、最低限次を確認する。

```bash
git status
git diff
```

- Markdown変更: リンク、見出し、表、コードブロックの表示
- n8n変更: JSONの構文、import可否、主要経路とエラー経路
- DB変更: SQLの構文、適用順、権限、ロールバック
- 実装変更: 対象機能のテスト、lint、build

## 変更禁止事項

- Issue範囲外のファイルや挙動を変更しない。
- 秘密情報、`.env`、生成された実行データをコミットしない。
- 基準ブランチへの直接コミット、force push、履歴を書き換える操作を行わない。
- テストを無効化して成功扱いにしない。
- 明示的な依頼なしにn8n workflow JSON、既存migration、本番データを変更しない。
