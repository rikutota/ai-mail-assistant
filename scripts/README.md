# Validation scripts

ローカルとCIで共用する、リポジトリ検証用スクリプトを配置します。

- workflow JSON、SQL、Markdown、fixtureなどの静的検証に限定する
- 既定では外部APIや本番データへ接続しない
- 検証中に秘密情報を標準出力やログへ出さない
- ファイルを書き換える処理は、検査コマンドと明確に分離する

このディレクトリを追加するIssueでは検証スクリプトを作成しません。
